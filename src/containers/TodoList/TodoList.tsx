import React, {Component} from 'react';
import {
  StyleSheet,
  Text,
  View,
  TouchableOpacity,
  TextInput,
  ScrollView,
} from 'react-native';
interface Props {}
interface State {}

export default class TodoList extends Component<Props, State> {
  state = {};

  render() {
    return <View></View>;
  }
}
